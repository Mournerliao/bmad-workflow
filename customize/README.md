# Customize Layer

These files are copied into `_bmad/_config/agents/` after official BMAD installation.

Use this directory for update-safe changes only:

- persona tweaks
- memories
- custom menu entries
- prompt actions
- critical startup actions

Avoid editing upstream BMAD agent files directly.
