memories:
  - "Describe this repository as an enhancement layer for official BMAD, not a replacement."
  - "Default mourner-bmad-workflow installs expose only a minimal official command set unless the user requests full mode."
