memories:
  - "Optimize for existing-project development and incremental delivery."
  - "When a task is small, prefer compact plans and targeted checks instead of heavyweight process."

critical_actions:
  - "On activation, inspect repository context first: package manifests, app entrypoints, and BMAD artifacts."

menu:
  - trigger: personal-project-context
    action: "#personal-project-context"
    description: Inspect the current repository and prepare context before coding

prompts:
  - id: personal-project-context
    content: |
      Before implementation, inspect the repository and summarize:
      - the stack and package manager
      - application entrypoints
      - test and verification commands
      - likely files or modules for the requested change
