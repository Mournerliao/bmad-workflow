---
name: bmad-generate-project-context
description: Generate BMAD project context for the current codebase.
---

# /bmad-generate-project-context

Use the official BMAD `bmad-generate-project-context` workflow bundled in this plugin.

Open `skills/bmad-generate-project-context/SKILL.md` (relative to this plugin root) and follow it, including any linked step files under `skills/bmad-generate-project-context/`, until the user's request is complete.

Do not rely on a separate "skill" activation: the workflow content is only in those files.
